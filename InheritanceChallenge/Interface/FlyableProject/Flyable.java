package InheritanceChallenge.Interface.FlyableProject;

public interface Flyable { //If we define the class as interface then we do not need od public and abstract we only define the return type and method name.
    void fly();
}
